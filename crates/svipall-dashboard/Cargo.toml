[package]
name = "svipall-dashboard"
version.workspace = true
repository.workspace = true
edition = "2021"
license = "AGPL-3.0-only"
description = "Human dashboard for manual captcha solving"

[dependencies]
svipall-solver = { path = "../svipall-solver", version = "1.0.0-rc.3" }
tokio = { workspace = true }
axum = { workspace = true }
serde_json = { workspace = true }
svipall-core = { path = "../svipall-core", version = "1.0.0-rc.3" }

[dev-dependencies]
tower = { workspace = true }
