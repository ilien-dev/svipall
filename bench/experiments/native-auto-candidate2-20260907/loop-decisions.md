# Improvement-loop decisions

The final comparison and audit are complete. This journal records retained and rejected changes;
[final-findings.md](final-findings.md) gives the measured tradeoffs and practical stopping decision.

| Hypothesis | Evidence | Decision |
|---|---|---|
| Skip weaker emulated tiers after a classified fingerprint/hold wall within a call | Candidate-1 local regression changes HTTP/browser/stealth/real to HTTP/real with the same useful response; core/MCP tests and QC pass | Retained. Candidate-1 public primary useful counts were 164 auto / 161 native, but its long interruption and overlapping round ranges prevent a causal gain claim |
| Remember recent classified walls across calls | Expiring, backward-compatible route evidence; local second-call regression starts at real in one attempt; expiry, later success, opt-out and ceilings tested | Retained in candidate 2. Strongest permitted emulation remains and native stays last; the completed public comparison does not establish a causal patch gain |
| Reuse the existing managed-challenge discriminator in automatic transitions | Core regression failed before the change; two-attempt local browser fixture succeeds through HTTP/real; all 12 core routing tests and manual integrations pass | Retained in candidate 2. No deadline, admission budget or native-order change |
| Preserve neutral wrappers around a listing heading with substantial enclosing prose | Original fixture loses five titles before the change and preserves all five afterward; hidden/navigation controls pass; full output unchanged | Retained narrow correction. Corpus median/mean remain 0.920/0.831 at displayed precision; 216 fewer reachable gold words lost, with a small vote-mean regression. Actual job/search captures show no restoration, so no broad listing-completeness claim |
| Preserve all neutral single-heading wrapper structures | Three saved public documents gain records, all 130 local tests pass, full output unchanged; complete 3,975-page diagnostic shows 65 improvements and 458 regressions, median/mean 0.919481/0.830188 versus 0.919792/0.830820 | Rejected. Unrelated news/navigation leakage reduces global precision. Isolated source, binaries and failed result retained |
| Require independent record description for the broader wrappers | New navigation regression fails before refinement; 131 local tests and Clippy pass; public record gains remain incomplete; all 3,975 pages show 50 improvements and 123 regressions, median/mean 0.919679/0.830703 | Rejected. Smaller global regression remains a regression; no broader code integrated |
| Accept short nonempty pages earlier in native warm mode | Local delayed-rendering control returns only Loading under premature acceptance but yields its completed report after warm waiting | Rejected as a blanket shortcut. Text length does not establish readiness; no warm-time change retained |

The retained candidate passed full current-tree QC: 1,216 workspace tests, ten manual automatic/
learning/timeout tests, four additional local browser tests, feature-matrix Clippy, ONNX and HTTP/3
checks, 160 browser probes, identity coherence, CPU/structural budgets and the available corpus
floors. All product inputs match that QC snapshot; the later root README-only documentation
change is recorded in `confirmation-readiness.json`. Six alternating before/after CPU-gate
invocations also passed. Fixture timing changes are not isolated causal effects of one patch.

The final executable and 918-call schedule are frozen in `manifest.json`, with accounting carried
forward from the three recorded HTML captures. No failed visit, cooldown or traffic charge is
removed. Every exact content fingerprint requires an explicit audit or transfer from an already
reviewed identical URL/status/content fingerprint. Useful excerpts in blocked responses remain a
secondary endpoint; they do not silently replace the prespecified useful-delivery measure.

Round 1 also exposed a controller-only Windows sharing failure on progress-file replacement.
The three existing requests completed and were recovered without replay or unknown admissions.
A bounded atomic-write retry now passes a regression that failed before the change, plus a
permanent-denial test that proves the previous complete JSON survives and retries stop. All
18 Python controller/report checks pass. The 152.42-second unplanned recovery delay is recorded
in `interruption-recovery.json`; it does not justify changing the frozen product or hiding the
interruption. Final results must include the affected-target sensitivity.

The completed comparison returned 130/129 useful deliveries and 131/136 available useful content
(auto/native), with 8.40/16.58 production seconds per useful result. All 287 fingerprints are
reviewed and all 918 published calls independently verified. The final report accounts for all
20 auto losses: 16 local deferrals, one deadline before native and three remote query-quota
responses. It records a practical stopping point for these tested hypotheses and constraints,
without claiming an absolute algorithmic ceiling, causal patch gain or statistical equivalence.
